name: ci

on: [push]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
    
    steps:
    - uses: actions/checkout@v1
    - name: dotnet info
      run: dotnet --info
    - name: test
      run: ./cake.ps1 -target=pack
